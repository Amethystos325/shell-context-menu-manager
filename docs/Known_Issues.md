# 已知问题（MVP）

文档日期：2026-02-07

1. 目前解析器为 MVP 子集，不保证覆盖全部 Nilesoft Shell 高级语法。
2. `apply` 自动执行依赖系统可用的 `shell` 命令；不可用时会回退到手动指引。
3. 安装包流程已提供脚本，但签名、图标、发行元数据仍需补齐。
4. 目前 `type/mode` 字段仍为文本输入，枚举下拉和智能提示尚未完善。
5. 复杂大文件下，源码实时同步存在性能波动风险（建议后续做增量同步优化）。
